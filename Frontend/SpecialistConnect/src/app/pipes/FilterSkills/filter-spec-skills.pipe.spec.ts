import { FilterSpecSkillsPipe } from './filter-spec-skills.pipe';

describe('FilterSpecSkillsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSpecSkillsPipe();
    expect(pipe).toBeTruthy();
  });
});
