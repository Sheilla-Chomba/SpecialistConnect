import { FilterRoleUserPipe } from './filter-role-user.pipe';

describe('FilterRoleUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRoleUserPipe();
    expect(pipe).toBeTruthy();
  });
});
